## REMOVED Requirements

### Requirement: 交易时段轮询门控

**Reason**: 用户场景需要在休市/周末/盘前盘后也能查看上一交易日收盘数据并感知盘前盘后波动（港美股交易时段与 A 股不同；用户也希望在 A 股休市时仍可查看持仓快照）。"仅交易时段轮询"假设过于狭窄。

**Migration**: 调度器不再基于交易时段判定是否轮询；仅保留视图可见性暂停（TreeView + StatusBar 都不可见时仍暂停）。`tradingHours.ts` 模块保留但不被调度器消费，以备未来"交易时段加速、休市降频"等增强使用。
